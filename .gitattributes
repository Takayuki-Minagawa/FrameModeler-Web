# Repository source files use CRLF historically. Treat the carriage return as part
# of the line ending so whitespace checks still catch real trailing spaces.
* whitespace=trailing-space,space-before-tab,cr-at-eol
