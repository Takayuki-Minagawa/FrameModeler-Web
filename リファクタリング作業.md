# リファクタリング作業計画 — FrameModeler-Web

作成日: 2026-06-22
対象: `src/` 全32ファイル / 約3,300行 (TypeScript + Three.js)

## 1. 目的とスコープ

C# WinForms 由来の建築構造フレームモデリング CAD を TypeScript + Three.js へ移植した本プロジェクトについて、機能を変えずに保守性・型安全性・性能・拡張性を改善するためのリファクタリング計画である。

本計画は、コードベースを 5 領域に分割し、各領域を専門のサブエージェントで並列分析した結果をメインで統合・優先度付けしたものである。

| 領域 | 主対象 | 行数規模 |
|------|--------|----------|
| データ層 | `src/data/`, `src/math/` | 約 800 行 |
| IO 層 | `src/io/` (Json Serializer/Deserializer) | 約 310 行 |
| 描画・ビュー層 | `src/ui/CadView.ts`, `main.ts`, `Layer.ts` | 約 1,000 行 |
| マウスハンドラ層 | `src/ui/handlers/` 9 ファイル | 約 460 行 |
| ダイアログ・i18n 層 | `src/ui/dialogs/`, `i18n.ts` | 約 500 行 |

---

## 2. 最優先: バグ級の問題（リファクタリング前に確認・修正）

リファクタリング作業の前提として、分析中に発見された「設計意図と実装の乖離」「実機能不全」を先に潰す。これらは挙動変更を伴うため、リファクタリングとは分離して扱う。

| # | 問題 | 場所 | 影響 | 工数 |
|---|------|------|------|------|
| B-1 | **Node 削除の参照チェックが未配線**。`Document.remove()` は `DocumentData.isRemovable()`（常に true）を呼ぶだけで、正しいロジックを持つ `Document.checkNodeRemovable()` はどこからも呼ばれていない（孤児メソッド）。CLAUDE.md 重要ルール 3 に直接違反し、参照中 Node が削除可能になっている | `Document.ts:72-87`, `Document.ts:310-321`, `DocumentData.ts:7-9` | データ整合性破壊 | S |
| B-2 | **ダークモードが Canvas 描画に未反映**。背景 `0xffffff`・グリッド・要素色がハードコードされ、README 記載のダークモード機能が 2D/3D ビューに追随しない | `CadView.ts:68,444,448-449,467,493-499` | 主要機能の不全 | M |
| B-3 | **保存ファイル名の二重拡張子**。JSON 移行後も保存時に `.xml` 置換が残存。`.json` を開いて保存すると `model.json.json` になり得る | `main.ts:199` | 軽微だが UX 不具合 | S |

> B-1 は最優先で挙動を確認すること。

---

## 3. 横断的テーマ（複数領域にまたがる構造的改善）

個別修正より先に方針を固めるべき、領域横断の論点。

### T-1. `onEndDrag` の `as any` ダックタイピング廃止 ★低リスク・高効果
`SelectionHandler.onEndDrag` は `ICadMouseHandler` に存在せず、`CadView` が `as any` + 実行時 `typeof` で呼んでいる（`CadView.ts:349-352`）。
→ `ICadMouseHandler` に `onEndDrag?(view, pos, event): void` をオプショナル追加し、`this._handler.onEndDrag?.(...)` で呼ぶ。描画層・ハンドラ層の両分析が独立に指摘した最有力候補。**工数 S / 着手最優先。**

### T-2. 型レジストリの導入（IO ⇄ Document の知識重複解消）
データ型の追加には現状 6 箇所以上の同期編集が必要（Serializer の instanceof チェーン、Deserializer の per-array ループ、`JsonDocument` interface、`normalizeDocument`、`Document.TYPE_ORDER`、`assignNumbers`/`compareData`）。`BearWall`/`Wall` のソート順も 2 箇所に重複。
→ コンストラクタをキーに `{ key, codec(toJson/fromJson), sortOrder, idOffset, defaults }` を 1 つのレジストリに集約し、IO と Document の両方をそこから駆動。**umbrella リファクタ。工数 L。** 後述の IO 系・データ系の個別項目を多数吸収する。

### T-3. `compareTo` の基底メソッド化
`Document.compareData` は型ごとの instanceof チェーンで `compareTo` を呼び分けている（`Document.ts:93-107`）。`compareTo` の引数型もサブクラスごとに非対称で型危険。
→ `DocumentData` に `compareTo(other: this): number`（デフォルト番号比較）を定義し、`compareData` は idx 比較後に一律 `a.compareTo(b)` を呼ぶだけにする。

### T-4. 死蔵 `save()`/`load()` 群の削除
XML 時代の `save(writer)`/`load(reader)` が `DocumentData`/`Node`/`Member`/`Plane`/`Floor`/`Wall` に残存し、JSON IO からは一切呼ばれない（grep 確認済み）。フィールド知識が IO 層と二重化しドリフトの温床。
→ grep で XML 呼び出し皆無を再確認のうえ全削除。XML/writer/reader 名残のコメントも一掃。

### T-5. `Document.getOrCreateNode(pos)` の新設
「座標に Node があれば取得、なければ生成して add」が全 Add 系ハンドラ 6 箇所に散在（本来 Document の責務）。デシリアライザの線形 `find` も同根。
→ `Document` に集約。`getNodeAbove`（get なのに add する副作用）も `getOrCreateNodeAbove` へ改名し意図を明示。

### T-6. 色・サイズ・閾値の定数集約（パレット / Config）
Canvas 内の色（背景・グリッド・軸・選択・要素）、サイズ（node 6, point 8, linewidth 2）、感度・閾値（hitRange 0.008, dblclick 400ms/10px, zoom 1.15, 共線判定 0.999）、プレビュー色がコード全体に散在。
→ `Theme`/`Palette` と `CadConfig` に集約。B-2（ダークモード）の前提にもなる。ハンドラ層のプレビュー色も同じ枠組みへ。

---

## 4. 領域別リファクタリング項目

### 4.1 データ層 (`src/data/`, `src/math/`)

| ID | 項目 | 場所 | 優先 | 工数 |
|----|------|------|------|------|
| D-1 | `chooseData<T>` の `as unknown as T[]` を型ガード（コンストラクタ型引数 + `filter((d): d is T => ...)`）に置換 | `Document.ts:40-42` | 高 | S |
| D-2 | `Member` の `nodeI!`/`nodeJ!` 非 null assertion 排除（2 ノード必須化、または `ok` ナローイング統一） | `Member.ts`, `Beam.ts`, `Pillar.ts` | 高 | M |
| D-3 | 削除可否ロジック 2 系統を統合し戻り型を `RemovableResult` 型に（B-1 と一体） | `Document.ts:310`, `DocumentData.ts:7` | 高 | S |
| D-4 | `existsOn`（レイヤー Z 範囲判定）の 3 重複を基底 `zRange()` に集約 | `Node.ts:18`, `Member.ts:44`, `Plane.ts:63` | 中 | S |
| D-5 | `compareTo` 内の z→y→x 段階比較を `compareNumbers([...])` ユーティリティに抽出 | `Node/Beam/Pillar/Floor.ts` | 中 | S |
| D-6 | `Member`/`Plane` の `getNode`/`setNode`/`isReferring` シグネチャ統一（共通 `HasNodes` インターフェース） | `Member.ts:34`, `Plane.ts:27` | 中 | M |
| D-7 | シングルトンの `onChanged`/`onLayerChanged` を単一スロット → 購読リスト（簡易 EventEmitter）化、テスト用 reset 提供 | `Document.ts:20` | 中 | M |
| D-8 | `suppressDataSort` フラグを `withSuppressedSort(fn)`（try/finally 保証）か `reindex()` 集約に | `Document.ts:26,65,82,293` | 中 | S |
| D-9 | `Point2D`/`Point3D` のベクトル演算重複を共通化（crossProduct の戻り型差に注意）、`Point3D.average()` 追加 | `Point2D.ts`, `Point3D.ts`, `Plane.ts:43`, `Document.ts:212` | 中 | M |
| D-10 | `getPosAbove` 長大メソッドを `findNodeAbove`/`findMemberIntersectionAbove` に分割、閾値 `0.999` を定数化 | `Document.ts:151-192` | 低 | M |
| D-11 | `Plane.ok` の無意味な null チェック是正、`Wall.wallLength` を `Point3D.sub().length` で再実装 | `Plane.ts:39`, `Wall.ts:15` | 低 | S |

### 4.2 IO 層 (`src/io/`)

> 多くが T-2（型レジストリ）で吸収される。レジストリ導入前に着手する場合の単独項目を以下に示す。

| ID | 項目 | 場所 | 優先 | 工数 |
|----|------|------|------|------|
| I-1 | `(FloorDirection as any)[...]` を型付き `parseFloorDirection()` に（IO と `Floor.load` 双方） | `JsonDeserializer.ts:98`, `Floor.ts:59` | 高 | S |
| I-2 | 要素レベルの型ガード追加（`isJsonNode`/`isJsonMember`/`isJsonPlane`）。現状 `jn.pos.x` 等が未検証で TypeError/NaN を誘発 | `JsonDeserializer.ts:130-162` | 高 | M |
| I-3 | ノード参照解決を `Map<number,Node>` 化（線形 `find` の O(n·m) 解消、番号重複検出） | `JsonDeserializer.ts:164-178` | 中 | S |
| I-4 | Plane の最小ノード数検証を load 時に（Floor≥3, Wall/BearWall≥2）。現状はソート時に遠隔でクラッシュ | `JsonDeserializer.ts:92-122` | 中 | S |
| I-5 | section 既定値の二重管理解消（`'G1'`/`'C1'`/`'S1'` を IO で再ハードコードせずクラス既定を参照） | `JsonDeserializer.ts:203-207` ほか | 中 | S |
| I-6 | `BearWall`-before-`Wall` の instanceof 順序依存を `constructor ===` 厳密判定に | `JsonSerializer.ts:57-65` | 中 | S |
| I-7 | `JSON.parse` 例外を統一エラー型でラップ、`parse→validate→build→commit` 段階を明示 | `JsonDeserializer.ts:58-128` | 中 | M |
| I-8 | bulkLoad のレイヤー重複（posZ）検証追加（`addLayer` の不変条件をバイパスしている） | `JsonDeserializer.ts:125` | 低 | S |
| I-9 | `serialize ∘ deserialize` 往復テストを追加し `isNodeReverse` 再計算・section 既定の非対称をガード | IO 全般 | 中 | M |

### 4.3 描画・ビュー層 (`src/ui/CadView.ts`, `main.ts`)

| ID | 項目 | 場所 | 優先 | 工数 |
|----|------|------|------|------|
| V-1 | **God Class 解体**: `CadView`(626行) から `CadRenderer`(描画 約220行) を分離。README/CLAUDE.md 想定の構成に合わせる | `CadView.ts:374-595` | 高 | L |
| V-2 | `CameraController` を分離（Ortho/Perspective、球面座標、pan/rotate/zoom/fit、screenToWorld の 2D/3D 分岐集約） | `CadView.ts:138-203,358-365` | 高 | L |
| V-3 | **毎フレーム全シーン再生成の解消**: dirty フラグでカメラ操作時は `renderer.render()` のみ。シーン再構築と描画を分離 | `CadView.ts:338,363,369-383` | 高 | M |
| V-4 | 要素ジオメトリのバッチ化（全 Node を 1 Points、全 Member を 1 LineSegments）、マテリアル共有、選択は color attribute 更新 | `CadView.ts:460-595` | 中 | L |
| V-5 | `InputController` 分離（DOM イベント・ドラッグ状態・dblclick 検出をカメラ/ツール意図に振り分け） | `CadView.ts:288-365` | 中 | M |
| V-6 | `disposeObject()` 共通化（Mesh/Line/Points の dispose コピペ統合、配列マテリアル対応） | `CadView.ts:385-402` | 中 | S |
| V-7 | preview Line/Points を使い回し（`geometry.setFromPoints` で頂点更新）、mousemove ごとの生成停止 | `CadView.ts:408-423` | 中 | M |
| V-8 | Ortho frustum 計算の 2 重複を `updateOrthoFrustum()` に統一、`up` 設定の意図をコメント化 | `CadView.ts:144-151,183-193` | 中 | S |
| V-9 | `layerZ` ゲッター集約（`shownLayer?.posZ ?? 0` の 4 箇所重複）、3D レイキャストを `Raycaster.intersectPlane` で簡潔化 | `CadView.ts:219-246,429` | 低 | S |
| V-10 | グリッド線本数の上限/LOD 導入（ズームアウト時の大量生成を抑制） | `CadView.ts:427-450` | 中 | M |
| V-11 | `requestAnimationFrame` ベースの描画スロットリング（`requestRender()` で dirty 集約、`getBoundingClientRect` キャッシュ） | `CadView.ts` 全 render 呼び出し | 中 | M |
| V-12 | `main.ts` の DOM 参照集約（`byId<T>(id)` ヘルパー、ID 定数化、20+ 箇所の `getElementById`+キャスト解消） | `main.ts` 多数 | 中 | M |
| V-13 | `main.ts` のトップレベル副作用を `App.init()` + UI コンポーネント（Toolbar/LayerPanel/StatusBar）に構造化 | `main.ts` 全体 | 中 | L |
| V-14 | `updateLayerList` をイベント委譲化し `onLayerChanged` 一本化（7 箇所の手動呼び出し・全再生成を削減） | `main.ts:166-344` | 中 | M |
| V-15 | ハンドラ生成 switch のボイラープレート削減（`{id: factory}` マップ + ダイアログ注入 API 統一、T-1 と連動） | `main.ts:86-131` | 中 | M |

### 4.4 マウスハンドラ層 (`src/ui/handlers/`)

| ID | 項目 | 場所 | 優先 | 工数 |
|----|------|------|------|------|
| H-1 | **`AddWallHandler` と `AddBearWallHandler` のほぼ完全重複を統合**（生成型を factory 引数化、`createQuadPoints` を共有モジュールへ） | `AddWallHandler.ts`, `AddBearWallHandler.ts` | 高 | S |
| H-2 | 2 点クリック系（Beam/Floor/Wall/BearWall）の状態機械を `TwoClickAddHandler` 基底（テンプレートメソッド）に集約。第 1 点の型はジェネリクスで切替 | 4 ハンドラ | 高 | M |
| H-3 | `onEndDrag` の型化（T-1） | `SelectionHandler.ts:73`, `CadView.ts:349` | 高 | S |
| H-4 | `getOrCreateNode` スニペット重複の解消（T-5 へ委譲） | Add 系 6 箇所 | 高 | S |
| H-5 | プレビュー閉ループ描画の重複を `view.addPreviewPolygon(points)` に集約 | Floor/Wall/BearWall/Selection | 中 | S |
| H-6 | `showDialog` 注入方法の統一（setter vs public フィールドの 2 系統 → `setDialogCallback` に統一） | 全ハンドラ + `main.ts:89-127` | 中 | S |
| H-7 | `ICadMouseHandler` にライフサイクル `onDeactivate?(view)`/`reset()` 追加（ツール切替・Esc キャンセルで状態クリア） | `ICadMouseHandler.ts` | 中 | M |
| H-8 | `isInsideRect` の `nodeI! as any` を型ガード（`ok(): this is ...` 型述語）で除去 | `SelectionHandler.ts:125-126` | 中 | S |
| H-9 | Document シングルトンへの密結合を `view.document` 経由 or DI に（テスト容易化） | 全ハンドラ冒頭 | 中 | M |
| H-10 | プレビュー色・矩形閾値（`MIN_RECT_SIZE`）等のマジックナンバーを `handlers/constants.ts` に集約（T-6 と連動） | 各所 | 中 | S |
| H-11 | `alert()` 直書きを `view.notify(messageKey)` 経由に（i18n 化、UI 層との脱密結合） | Beam/Pillar/Floor/Wall/BearWall | 低 | S |
| H-12 | 基準点変数名の統一（`prevNode`/`prevPoint`/`prevPos`/`dragStartWorld` → `anchor`/`firstPoint`）、H-2 で吸収 | 各ハンドラ | 低 | S |

### 4.5 ダイアログ・i18n 層 (`src/ui/dialogs/`, `i18n.ts`)

| ID | 項目 | 場所 | 優先 | 工数 |
|----|------|------|------|------|
| G-1 | **ダイアログ共通ライフサイクルの抽出**: `wireDialog(overlay, okBtn, cancelBtn, onOk)` 高階関数で `showDialog`/`closeDialog`/`resolve` 配線重複を解消（4 ダイアログが数行に圧縮） | Member/Node/Plane/LayerDialog | 高 | M |
| G-2 | **i18n 翻訳キーの型安全化**: `messages` を `as const` 化し `type MessageKey = keyof typeof messages`、`t(key: MessageKey)`。未定義キーをコンパイルエラー化 | `i18n.ts:8,112` | 高 | M |
| G-3 | クローズ処理を `dismiss(result)` に集約（リスナ解除 + remove + resolve をセット保証、Promise リーク予防） | 全ダイアログ | 中 | S |
| G-4 | ESC / オーバーレイクリック閉じの統一（現状 Help のみ実装）。`wireDialog`/`showModal` に共通組込み | 全ダイアログ | 中 | M |
| G-5 | `DialogUtil` の二層分離（DOM 部品 `DialogForm` ⇔ 制御 `wireDialog`）、未使用 `DialogResult` 削除 | `DialogUtil.ts` | 中 | M |
| G-6 | 読み取り専用ノード行 `${n.number} (${n.pos})` を `addNodeRow()` に共通化 | `MemberDialog.ts:14`, `PlaneDialog.ts:23` | 低 | S |
| G-7 | ハードコードラベル（`NodeI`/`X`/`Y`/`Z`）と方向配列 `['X','Y','XY']` の集約（`FloorDirection` から導出） | Member/Node/PlaneDialog | 低 | S |
| G-8 | `updateDom` の 4 種 data 属性処理をテーブル駆動化、非 null assertion/キャスト削減 | `i18n.ts:138-169` | 低 | M |
| G-9 | `innerHTML` 禁止の規約明記（現状 XSS なし＝`textContent` 統一を維持） | 全般 | 低 | S |

---

## 5. 推奨実施フェーズ

挙動変更（バグ修正）→ 低リスク高効果 → 構造改革 → 仕上げ、の順で進める。各フェーズ末でビルド（`npm run build`）と手動動作確認を行う。

### フェーズ 0: バグ修正（挙動変更を伴う / 個別 PR）
- B-1 Node 参照チェック配線 / B-3 拡張子置換
- B-2 はパレット集約（T-6）に依存するためフェーズ 2 で扱う

### フェーズ 1: 低リスク・高効果（型安全 + デッドコード + 即効重複）
- T-1/H-3 `onEndDrag` 型化
- T-4 死蔵 `save`/`load` 削除
- D-1 `chooseData` 型ガード / I-1 `parseFloorDirection` / H-8 型述語
- H-1 Wall/BearWall 統合
- I-3 ノード解決 Map 化 / I-4 Plane ノード数検証 / I-2 要素型ガード
- G-2 i18n 型安全化

### フェーズ 2: 共通基盤の抽出（横断テーマ）
- T-5 `getOrCreateNode` / H-4 委譲
- T-6 パレット・Config 集約 → **B-2 ダークモード反映**
- T-3 `compareTo` 基底化 / D-3+B-1 削除可否統合
- G-1+G-3+G-4 `wireDialog`/`dismiss`/ESC 統一
- H-2 `TwoClickAddHandler` 基底（H-5/H-6/H-10/H-12 を吸収）
- V-6 `disposeObject` / V-8 frustum 統一

### フェーズ 3: 構造改革（God Class 解体・性能）
- V-1 `CadRenderer` 分離 / V-2 `CameraController` 分離 / V-5 `InputController` 分離
- V-3 dirty レンダリング / V-4 ジオメトリバッチ化 / V-7 preview 使い回し / V-11 rAF スロットリング
- V-12〜V-15 `main.ts` 構造化
- T-2 型レジストリ（IO 個別項目 I-5/I-6 と Document 採番を吸収）

### フェーズ 4: 仕上げ
- D 系残（D-4〜D-11）/ I-7〜I-9 / V-9/V-10 / H-7/H-9/H-11 / G-5〜G-9
- I-9 往復テスト等のテスト整備

---

## 6. 優先度・工数サマリ

| 優先度 | 件数 | 代表項目 |
|--------|------|----------|
| バグ | 3 | B-1 参照チェック未配線, B-2 ダークモード未反映, B-3 二重拡張子 |
| 高 | 12 | T-1 onEndDrag, T-2 型レジストリ, V-1/V-2 God Class 解体, V-3 再描画, G-1 wireDialog, G-2 i18n 型化, H-1/H-2 ハンドラ統合, D-1〜D-3, I-1/I-2 |
| 中 | 30 程度 | 各領域の重複解消・分離・定数集約 |
| 低 | 12 程度 | 命名統一・規約化・細部 |

工数表記: S = 〜0.5 日 / M = 0.5〜1.5 日 / L = 2 日以上（いずれも目安）。

---

## 7. 進め方の注意（CLAUDE.md ルール準拠）

- Document はシングルトンで dataList を一元管理する設計を維持（T-2/D-7 でも破壊しない）。
- データ追加時の自動ソート→番号再割当（`reindex` 一体化）の不変条件を崩さない。
- Node 削除前の参照チェックを**実際に機能させる**（B-1）。
- Point3D は `{x, y, z}` オブジェクト形式の JSON 保存を維持（往復テスト I-9 で保証）。
- 各フェーズは小さな PR に分割し、`npm run build` 成功とサンプルデータ（`pillar_test.json` / `test.json`）での手動確認をゲートとする。

---

## 8. 実施結果（2026-06-22 完了）

ブランチ `refactor/framemodeler-cleanup` で本計画を実装した。回帰保護として Vitest を導入し（DOM 非依存の math/data/io 層）、全フェーズを小さなコミットに分割。各コミットで `npm run build`（tsc + vite）と `npm test` をゲートとした。**最終状態: ビルド成功・全 68 テスト green・独立レビューで確実な回帰 0 件**。

### コミット構成（フェーズ順）
- テスト基盤（Vitest 50→68 テスト）
- フェーズB: B-1 参照チェック配線 / B-3 拡張子 / T-4 デッドコード削除
- フェーズC: T-1 onEndDrag 型化 / D-1 chooseData 型ガード / I-1〜I-4 IO 堅牢化 / H-8 / G-2 i18n 型安全化
- フェーズD: T-5 getOrCreateNode / T-3+D-3 compareTo 基底化・削除可否統合 / T-6+B-2 パレット集約・ダークモード反映 / V-6 / V-8 / V-9 / V-10 / G-1/G-3/G-4/G-5 wireDialog / H-1/H-2/H-5/H-6/H-10/H-12 ハンドラ基底
- フェーズE: V-11+V-3 rAF 描画集約 / V-12 byId / V-14 イベント委譲 / T-2 型レジストリ+I-6 / **V-1/V-2/V-5 God Class 解体**（CadView 626→333行、CadRenderer/CameraController/InputController に分割）
- フェーズF: D-4/D-5/D-8/D-9/D-10/D-11 / I-7/I-8 / G-6/G-7/G-8 / H-7/H-11

### 意図的に見送った項目（理由付き）
ランタイム（ブラウザ/Three.js/DOM）を本環境で動作確認できないため、**「自動テスト不能 × 低 ROI × 挙動破壊リスク」**の項目は実施を見送り、ここに明示する。

| ID | 項目 | 見送り理由 |
|----|------|-----------|
| D-6 | Member/Plane の `HasNodes` 共通インターフェース | 中規模リファクタだがビルド検証のみ・実利が限定的 |
| D-7 | `onChanged` の購読リスト(EventEmitter)化 | 購読者は現状1つのみ。単一コールバックで充足、変更は破壊リスクに見合わない |
| H-9 | ハンドラの Document シングルトン脱密結合(DI) | 現状シングルトン前提で実害なし。テスト導入時に再検討 |
| V-7 | preview オブジェクトの使い回し | V-11(rAF 集約)で実用上の負荷は軽減済み。残りは限界的 |
| V-13 | main.ts の App クラス化 | 手続き的グルーで動作中。テスト不能・churn 大・ROI 低。byId/委譲/factory マップで部分改善済み |
| G-9 | innerHTML 禁止の規約化 | コード変更不要（現状 `textContent` 統一で XSS なし）。本ドキュメントに方針として明記 |

### レビューで確認された低リスク事項（許容）
1. **デシリアライズの最小ノード数強制**（I-4: floor≥3, wall/bearWall≥2）。退化した平面を含む旧ファイルはロード時に明示エラーとなる（従来は後続のソートで不可解にクラッシュしていたため、早期失敗のほうが望ましい）。
2. **`Plane.ok` の意味変更**（常に true → `length>0`）。空 Plane は描画/ヒットテストで元から除外されるため実害なし。

### テスト構成
`tests/`: Point3D / Point2D / Document / JsonRoundtrip / JsonValidation / PhaseF（計 68 テスト）。`npm test`（vitest run）で実行。

---

## 付録: 分析の出所

本計画は以下 5 領域を並列分析した結果を統合した。指摘の `file:line` 参照は分析時点のもので、着手前に最新コードで再確認すること。

1. データ層 — Document シングルトン / 継承構造 / 採番・参照チェック
2. IO 層 — Json Serializer/Deserializer の対称性・型安全・拡張性
3. 描画・ビュー層 — CadView God Class / 再描画性能 / 色ハードコード
4. マウスハンドラ層 — Add 系重複 / 状態機械 / インターフェース設計
5. ダイアログ・i18n 層 — ライフサイクル重複 / 翻訳キー型安全
